import { build } from 'esbuild';
import fs from 'fs';
import path from 'path';
import temp from 'temp-dir'; // 使用临时目录
import createEsbuildPlugin from '../src';

describe('esbuild-plugin-iife', () => {
});
